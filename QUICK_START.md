# Quick Start

1. Clone: git clone https://github.com/miltorg/todo-api.git
2. Setup: cd todo-api && composer install
3. Configure: cp .env.example .env && php artisan key:generate
4. Database: touch database/database.sqlite && php artisan migrate
5. Run: php artisan serve
6. Test: Open http://localhost:8000/test
